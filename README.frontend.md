# 前端 React 应用

## 启动说明

```bash
npm install
npm run dev
```

## 环境变量

- `VITE_WS_URL`：WebSocket 地址
- `VITE_API_BASE_URL`：API 地址

## 项目结构

- `src/pages` 页面
- `src/components` 组件
- `src/hooks` 交互 Hooks
- `src/store` Zustand 状态
- `src/api` HTTP 客户端
- `src/types/shared.ts` 前后端共享类型
